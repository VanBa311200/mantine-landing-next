When finishing a coding task in this repo:
1) Run `npm run lint` to catch style/type-aware lint issues.
2) If changes affect runtime behavior, run `npm run build` for a production check.
3) Manually verify key landing page sections render in dev (`npm run dev`) when UI is touched.
4) Summarize changed files and any follow-up needed.